# Deck v3.2 — Design

> Iteration on `v3.1`. Seven small content fixes: drops three bullet/big-word pacing slides in Parts 2–3, merges a video into an adjacent bullet slide, replaces a thesis-BIG-WORD with a code-recall slide in Part 5, drops an "org-first" closer in Part 9, and rewrites two bullet lists (Shipped + closing Advice). Final slide count: **120** (was 125).
>
> Tag at end: `v3.2` (preserves `v3.1`).

---

## 1. The seven changes

### 1.1 · Drop slide 18 ("Thick client by design" BULLETS)

Current bullets ("Complex menu / Lots of customisation rules / Validation, pricing, stop-lists — client side / Seamless native feel") are noise — slide 19 ("Thick client. On purpose.") makes the same point in a single BIG-WORD. Drop.

### 1.2 · Drop slide 21 ("The price of thin" BULLETS)

Bullets restate slide 20's COMPARE in list form. Drop.

### 1.3 · Merge slide 24's video into slide 23, then drop slide 24

Currently:
- **23** = BULLETS "Our bet" (3 short "We want to *feel*…" bullets, no visual)
- **24** = IMAGE "Little things customers feel" — 4 bullets on the left + microinteraction-2x video on the right

Target: keep slide 23's "Our bet" title, move the microinteraction-2x video to its right column (same `.phone phone--real` layout as old slide 24). Drop slide 24 entirely.

Merged slide 23 markup outline:

```html
      <!-- 23 · IMAGE · Our bet (with brand-feel video) -->
      <section class="slide slide--content">
        <h3 class="slide__title">Our bet</h3>
        <div class="slide__body" style="display:grid; grid-template-columns: 1.2fr 1fr; gap: 56px; align-items: center; height: 82%;">
          <ul class="list list--lg stagger">
            <li>We want to <em>feel</em> native.</li>
            <li>We want to <em>feel</em> instant.</li>
            <li>We want to <em>feel</em> different from everyone else.</li>
          </ul>
          <div class="phone phone--real anim-fade" style="margin: 0 auto;">
            <div class="phone__screen">
              <video src="assets/media/microinteraction-2x.webm"
                     autoplay loop muted playsinline
                     class="phone__shot"></video>
            </div>
          </div>
        </div>
      </section>
```

Slide TYPE changes from BULLETS to IMAGE (since it now carries a video on the right). Speaker note merges the two original notes briefly.

### 1.4 · Drop slide 25 ("Will it pay off?" BIG-WORD)

Current slide 25: BIG-WORD "Will it pay off?" — vestigial since slide 26 ("But the bet has a price") and slide 27 ("And this is where it starts to hurt." TRANSITION) carry the next beat. Drop.

### 1.5 · Replace slide 61's content with a code-recall slide; keep 62, 63

Current slide 61 is a thesis BIG-WORD ("Don't replace a layer / Replace nothing / Add one feature"). Replace its content with a new COMPARE slide that **recalls the combo-rule duplication from slide 35** — same Kotlin-on-top, Swift-on-bottom code — but adds a callout headline "This is the problem. KMP fixes this."

The intent: turn the section opener from a thesis statement into a problem-recall punch. Slides 62 (module-graph base) + 63 (slice highlighted) follow unchanged, then slide 64 ("What hurts? Domain logic") delivers the resolution.

Replacement slide-61 markup:

```html
      <!-- 61 · COMPARE · Remember this duplication? KMP fixes this. -->
      <section class="slide slide--content">
        <h3 class="slide__title">Remember this code? <span style="color: var(--color-accent-purple);">KMP fixes this.</span></h3>
        <div class="slide__body" style="display:flex; flex-direction:column; gap: 10px; height: 86%;">
          <div class="anim-from-left">
            <p class="t-mono-bold" style="color: var(--color-accent-orange); margin: 0 0 4px; font-size: 11pt;">// android — Kotlin (live)</p>
<pre class="code-block" style="font-size: 11pt; line-height: 1.35; padding: 10px 16px; margin: 0;">[…same Kotlin pickSlotProduct snippet currently on slide 35…]</pre>
          </div>
          <div class="anim-from-right">
            <p class="t-mono-bold" style="color: var(--color-accent-purple); margin: 0 0 4px; font-size: 11pt;">// ios — Swift (also live)</p>
<pre class="code-block" style="font-size: 11pt; line-height: 1.35; padding: 10px 16px; margin: 0;">[…same Swift pickSlotProduct snippet currently on slide 35…]</pre>
          </div>
        </div>
      </section>
```

The code on the new slide 61 is **literally copied from slide 35** (Kotlin block from `.anim-from-left` and Swift block from `.anim-from-right`). Slide 35 stays as-is (the speaker references it twice in the talk, once when introducing the bug and once as the call-back).

Speaker note for the new slide 61: *"Remember this code from earlier? Both apps. Same logic. Written twice — and that's where the bugs lived. This whole section is about fixing exactly this pattern with KMP."*

### 1.6 · Drop slide 111 ("Solve the org first." BIG-WORD)

Current slide 111: "Solve the **org** first." Drop — the surrounding slides about Mobile Engineer culture already carry the point.

### 1.7 · Rewrite slide 114 ("Shipped") bullets

Current (3 bullets): 2 features in KMP / DrinkitLab / one more feature.

New bullets (5 items, more substance):

```html
      <!-- 114 · BULLETS · Shipped -->
      <section class="slide slide--content">
        <h3 class="slide__title">Shipped — in production today</h3>
        <div class="slide__body">
          <ul class="list list--lg stagger">
            <li><strong>2 features</strong> running in both apps — DrinkitLab + a combo-logic feature.</li>
            <li><strong>HTTP transport bridge</strong> + plumbing pattern in production use.</li>
            <li><strong>CI build &amp; release pipeline</strong> — artifacts published by tag, both apps consume.</li>
            <li><strong>Unit-test coverage</strong> on the shared code — meaningful, in CI.</li>
            <li>iOS now has tests on this surface for the <em>first time</em> — Android already did.</li>
          </ul>
        </div>
      </section>
```

Speaker note: *"What we have in production today. Two features running in both apps — DrinkitLab and one more on the combo side. The HTTP transport bridge we just saw, plus the plumbing pattern, in production use. A CI release pipeline that publishes the KMP artifacts by tag — both apps just consume them. And a meaningful unit-test layer on the shared code — which is a real change for iOS, because iOS historically had almost no unit coverage on this surface. Android already did; now both do."*

### 1.8 · Rewrite slide 123 ("If you take one thing…") bullets

Current (4 bullets): don't replace a layer / don't rewrite / start with one feature, domain only / invert every other dependency.

Replace with the speaker's *actual insight* — focus on the code you **write right now**, not on layers you could try to move. 3 short bullets:

```html
      <!-- 123 · BULLETS · The advice -->
      <section class="slide slide--content">
        <h3 class="slide__title">If you take one thing from this talk…</h3>
        <div class="slide__body">
          <ul class="list list--lg stagger">
            <li><strong>Stop asking "what can we move?"</strong> — every layer drags its dependencies; you end up rewriting everything.</li>
            <li><strong>Ask "what do we write twice, today?"</strong> — that's the answer.</li>
            <li>For Drinkit it was <strong>domain logic</strong>. For you, it might be something else.</li>
          </ul>
        </div>
      </section>
```

Speaker note: *"If you take one thing away from this talk, it's this. I spent years trying to figure out what we could move — data layer, domain layer, common modules — and every time the answer cascaded into 'we have to rewrite everything,' because every layer pulled its dependencies. The insight that finally worked: stop asking what we can move. Ask what we write twice, right now. For us that turned out to be domain logic. For you it might be something else — but the question is always the same."*

---

## 2. Slide count math (v3.1 → v3.2)

| Op | Δ |
|---|---|
| 1.1 — drop slide 18 | −1 |
| 1.2 — drop slide 21 | −1 |
| 1.3 — merge 23+24, drop 24 | −1 |
| 1.4 — drop slide 25 | −1 |
| 1.5 — replace 61's content (keep 62, 63) | 0 |
| 1.6 — drop slide 111 | −1 |
| 1.7, 1.8 — content edits | 0 |

Net: **−5**. Final count: **120**.

Cumulative renumbers (old v3.1 marker → new v3.2 marker):

| Old | New | Notes |
|---:|---:|---|
| 1–17 | 1–17 | unchanged |
| 18 | — | DROP |
| 19–20 | 18–19 | −1 |
| 21 | — | DROP |
| 22 | 20 | −2 |
| 23 | 21 | −2 (gains video) |
| 24 | — | DROP (merged into new 21) |
| 25 | — | DROP |
| 26–60 | 22–56 | −4 |
| 61 | 57 | −4 (content replaced) |
| 62–110 | 58–106 | −4 |
| 111 | — | DROP |
| 112–125 | 107–120 | −5 |

Final ranges (subject to verification at implementation time):

- Part 0 — Opener: 1–6
- Part 1 — Two different apps?: 7–14
- Part 2 — Why our client got fat: 15–23 (was 15–27, lost slides 18, 21, 24, 25)
- Part 3 — We write the same thing twice: 24–37 (was 28–41)
- Part 4 — KMP — just start, right?: 38–53 (was 42–57)
- Part 5 — One feature. Domain only.: 54–65 (was 58–69, slide 61 content replaced)
- Part 6 — When the feature needs more.: 66–76 (was 70–80)
- Part 7 — The HTTP transport socket.: 77–90 (was 81–94)
- Part 8 — How we ship it: 91–95 (was 95–99)
- Part 9 — Getting iOS engineers writing Kotlin: 96–107 (was 100–112, lost slide 111)
- Part 10 — Results & closing: 108–120 (was 113–125)

(Section-title eyebrow text on slide 91 stays `// part 08`; on 96 stays `// part 09`; on 108 stays `// part 10`. No bloom changes.)

---

## 3. Files to update — sync impact

| File | What changes |
|---|---|
| `presentation/index.html` | All slide changes per §1, full renumber cascade, no class/CSS additions. |
| `PRESENTATION_PLAN.md` | All slide entries per §1, slide-count summary table at the bottom, top-level `# PART N …` headers. |
| `iOS-ARCHITECTURE.md` | Slide pointer paragraph. v3.1 had `~14, ~46, ~48, ~49`. v3.2: slide 14 unchanged (Part 1, no shift); slide 46 → 42 (−4); slides 48, 49 → 44, 45. New pointer: `~14, ~42, ~44, ~45`. |
| `ASSETS.md` | Add a "v3.2 changes" section noting drops + the video-merge + bullet rewrites. No new assets, no removed assets. |
| `presentation/css/deck.css` | No changes. |

No changes to `code-examples/` (frozen).

---

## 4. Implementation outline — 6 commits

1. **`v3.2 part-2 cleanup`** — drop slides 18, 21; merge 24's video into 23; drop slide 24; drop slide 25; cascade-renumber 26..125 → 22..121. Net −4 (124-1 from 125 then -4 = 120 — but math says only -4 here, not -5). Wait — Part 5 op 1.5 is net 0, op 1.6 is -1. Let me re-check.

   Hmm — operations 1.1, 1.2, 1.3, 1.4 are net −4 (drop 4 slides). After this commit: 125 − 4 = **121**.

2. **`v3.2 slide 61 code-recall`** — replace slide 61 content with the COMPARE code-recall (using code from slide 35). No count change. After this commit: **121**.

3. **`v3.2 drop slide 111`** — drop the "org-first" BIG-WORD; cascade-renumber. After this commit: 121 − 1 = **120**.

4. **`v3.2 slide 114 shipped`** — rewrite bullets per §1.7. No count change.

5. **`v3.2 slide 123 advice`** — rewrite bullets per §1.8. No count change.

6. **`v3.2 sync`** — update `iOS-ARCHITECTURE.md` slide pointer, `ASSETS.md` v3.2 section, `PRESENTATION_PLAN.md` slide-count summary + PART headers; tag `v3.2`.

---

## 5. Risk register

| Risk | Mitigation |
|---|---|
| Code in new slide 61 drifts from slide 35 over time. | Source comment in slide 61 markup pointing at slide 35; in code-review of future edits, both must move together. |
| Merging video into slide 23 changes its pacing from 3-bullet (~6 sec) to 4-line layout with video (~15 sec). | Acceptable — the speaker can pause longer over the bet. |
| Renumber cascade introduces a marker gap. | Run the CLAUDE.md `awk '$1!=1'` check after every commit, exactly like v3 and v3.1 did. |
| iOS-ARCH slide-pointer numbers wrong. | Verify each pointer by `grep '<!-- N · '` before committing T6. |

---
