# Deck v3.1 — Design

> Iteration on the v3 deck (tag `v3`). Six small-to-medium changes:
> tighten the cover, drop redundant architecture detail, drop the
> placeholder pure-function diagram, reframe Part 6 to lose its
> AI-specific code in favor of a "dependency inversion / baby steps"
> narrative, and **add a brand-new Part 8 — "How to ship it"** about
> KMP distribution. Plus an avatar on the closing slide.
>
> Final slide count: **125** (was 123).
> Tag at end: `v3.1` (preserves `v3` for rollback).

---

## 1. The six changes

### 1.1 · Slide 1 — drop the event-poster inset, English-only date

The cover currently has an `<aside class="cover__event-card">` holding `ad.jpeg`. Drop the `<aside>`. Change the brand-line text:

- **Before:** `Bereke Android Meetup · GDG Almaty · 28 мая 2026`
- **After:** `Bereke Android Meetup · GDG Almaty · 28 May 2026`

The poster file `ad.jpeg` stays in the repo (already tracked; future iteration may reuse).

### 1.2 · Drop current slides 46 and 47 (Android arch + iOS arch detail)

Current Part 4 has:

| Slide | Content |
|---:|---|
| 43 | Tutorial promise (IMAGE) |
| 44 | Greenfield (DIAGRAM) |
| 45 | "We are not greenfield" (BIG-WORD) |
| 46 | Our Android architecture (DIAGRAM) — DROP |
| 47 | Our iOS architecture (DIAGRAM) — DROP |
| 48 | Module graph (DIAGRAM, `ios-android-modules.png`) — kept |

Slide 14 (the two-architectures-detailed PNG, early in Part 1) and slide 48 (the module graph PNG) together carry the architecture story. The detailed individual platform diagrams are redundant.

> **Note on user wording:** the user wrote "28 is enough already" which I'm reading as a slip — slide 28 is the Part 3 section title and doesn't carry architecture content. The substance ("slide 14 and 48 together are enough") is unambiguous, so I'm proceeding with dropping 46 + 47. Flag if I misread.

### 1.3 · Drop current slide 69 (placeholder pure-function diagram)

Slide 69 is a CSS-placeholder card with `data in → pure function → data out` text. It was kept in v3 alongside the new slide 70 (`pure-function-shared-domain.png`). With slide 70 carrying the picture, slide 69 is redundant. Drop it.

### 1.4 · Part 6 — replace AiService-specific code with a "dependency inversion / baby steps" beat

Current Part 6 (slides 75–83) ends with two CODE slides showing the actual `AiService` interface (slide 82) and the sealed `CreatedDrinkResult` (slide 83). These were the only slides where the audience saw real shared-code shape, but they're tied to a specific Drinkit AI feature. The user wants them gone (too specific for the audience) and replaced with a generic narrative beat.

New Part 6 structure (slide numbers refer to v3.1 post-renumber positions):

| v3.1 slide | Type | Content |
|---:|---|---|
| 72 | SECTION | "6 · When the feature needs more." (was 75) |
| 73 | IMAGE | DrinkitLab triptych (was 76) |
| 74 | BULLETS | What this one needs from the world (was 77) |
| 75 | BIG-WORD | "Same idea. More plumbing." (was 78) |
| 76 | DIAGRAM | We can't just lift data — `kmp-data-only-blocked.png` (was 79) |
| 77 | BULLETS | **NEW** — "Network · menu · stops · preferences. Not a pure function." |
| 78 | BIG-WORD | **NEW** — "Dependency inversion. Baby steps." |
| 79 | DIAGRAM | Network — invert it. `kmp-data-with-interfaces.png` (was 80) |
| 80 | DIAGRAM | Domain — same trick. `kmp-domain-data-with-providers.png` (was 81) |

Net Part 6 count change: **0** (insert 2, drop 2). Old slides 82 (AiService interface CODE) and 83 (CreatedDrinkResult sealed CODE) are deleted.

**Speaker notes for the two new slides:**

- New slide 77 (BULLETS): *"We need the network. We need the menu. We need stop-lists. We need user preferences. Same shared logic on both platforms — but now there's plumbing the KMP module can't own itself. This isn't a pure function any more."*
- New slide 78 (BIG-WORD): *"Same trick we used for combo, just one level up. Dependency inversion. Baby steps — solve one thing at a time."*

### 1.5 · NEW Part 8 — "How to ship it" (KMP distribution)

Insert a 5-slide section between current Part 7 (HTTP transport) and current Part 8 (Mobile Engineer). The new section's job: address the practical question of *how a single KMP codebase reaches both apps*, and surface the asymmetry that Android ships many Maven artifacts while iOS gets one umbrella framework.

Post-insert numbering:
- Part 7 (HTTP transport socket): ends at slide 94 (was 97 in v3)
- **NEW Part 8 (How to ship it):** slides 95–99 (5 slides)
- Part 9 (Getting iOS engineers — was Part 8): 100–112
- Part 10 (Results — was Part 9): 113–125

New Part 8 slide list:

| Slide | Type | Content (working titles) |
|---:|---|---|
| 95 | SECTION | **"8 · How we ship it"** (purple bloom or orange — TBD by rotation; spec says **mint** to maintain rotation purple→orange→blue→mint→purple→orange→blue→mint→**mint**? — see §3 below) |
| 96 | BULLETS | "Monorepo? Copy-paste? Library?" — 3 options listed; speaker calls library. |
| 97 | BIG-WORD | **"Library."** (with accent color) |
| 98 | DIAGRAM | `kmp-integration.png` — Android multi-artifact / iOS single framework. Caption explains why. |
| 99 | BIG-WORD | **"CI ships it."** — one beat closing the section. |

**Asset:** `presentation/assets/diagrams/kmp-integration.png` (already in the repo).

**Speaker notes:**

- 96: *"How do you actually ship Kotlin code so two apps can use it? Three options: monorepo with the apps, copy-paste the shared file into each app, or publish as a library and depend on it. We picked library."*
- 97: *"Library."*
- 98: *"Different shape on each platform. On Android, every Gradle module of our KMP project ships as its own Maven artifact — `kmp:combo`, `kmp:transport`, etc. Each app adds only what it needs and the JVM class loader makes the same Kotlin class equal across artifacts. On iOS, the practical reality is one umbrella framework. Why? Each Kotlin/Native framework bundles its own copy of the Kotlin runtime, and a class shipped in two different frameworks shows up as two different Swift types — not interchangeable. JetBrains explicitly recommends shipping one umbrella framework that re-exports the modules iOS needs. Internal modularity stays clean on the Kotlin side; iOS just sees one artifact."*
- 99: *"And the actual release: CI does it. New tag, new artifact set, both apps pick up the new version. Zero manual work."*

**Verification status of the Android/iOS-distribution claim:** Verified by deep-research subagent against JetBrains' official KMP docs (`kotlinlang.org/docs/multiplatform/multiplatform-project-configuration.html` + `…/multiplatform-build-native-binaries.html`) and Touchlab's "Multiple Kotlin Frameworks in an Application" — the claim is **mostly TRUE** with one nuance: shipping multiple Kotlin/Native frameworks isn't strictly *impossible*, it's officially discouraged because of (a) per-framework Kotlin runtime/stdlib duplication, (b) per-framework Kotlin classes becoming distinct Swift types, (c) duplicated transitive deps, (d) per-framework singletons. The speaker note above reflects this nuance.

### 1.6 · Closing slide (now 125) — add portrait avatar

Current closing slide: QR-code on left (220×220), name + role + 4 social handles on right.

Add `assets/portrait.jpg` as a circular avatar (~150–180px) at the top of the right column, above the "Max Kachinkin" name line. New layout becomes:

```
+---------+   ⌬  Max Kachinkin
|  QR     |      Dodo Engineering · Drinkit Mobile
|  220px  |      telegram @maxkachinkin
|         |      channel @mobilefiction
+---------+      linkedin /maxkachinkin
                 twitter /makzimi
```

CSS: avatar uses `border-radius: 50%; object-fit: cover; width: 96px; height: 96px;` — small enough not to crowd the text block.

---

## 2. Final slide-count math

Starting from v3 = 123:

| Op | Δ |
|---|---|
| 1.2 — drop slides 46, 47 | −2 |
| 1.3 — drop slide 69 | −1 |
| 1.4 — Part 6: insert 2, drop 2 | 0 |
| 1.5 — new Part 8 (5 slides) | +5 |

Net: **+2** → **125 slides**.

Old `v3` part counts and the new v3.1 part counts:

| Part | v3 range / count | v3.1 range / count |
|------|---|---|
| 0 Opener | 1–6 / 6 | 1–6 / 6 |
| 1 Two different apps? | 7–14 / 8 | 7–14 / 8 |
| 2 Why our client got fat | 15–27 / 13 | 15–27 / 13 |
| 3 We write the same twice | 28–41 / 14 | 28–41 / 14 |
| 4 KMP — just start, right? | 42–61 / 20 | **42–59 / 18** (−2) |
| 5 One feature. Domain only. | 62–74 / 13 | **60–71 / 12** (−1) |
| 6 When the feature needs more | 75–83 / 9 | **72–80 / 9** |
| 7 The HTTP transport socket | 84–97 / 14 | **81–94 / 14** |
| 8 *NEW: How we ship it* | — | **95–99 / 5** |
| 9 Mobile Engineer (was 8) | 98–110 / 13 | **100–112 / 13** |
| 10 Results & closing (was 9) | 111–123 / 13 | **113–125 / 13** |
| **Total** | **123** | **125** |

Section-title slides shift correspondingly: 7, 15, 28, 42, 60, 72, 81, **95**, 100, 113. Bloom colors continue the purple→orange→blue→mint rotation; the new Part-8 section title (slide 95) takes the next color in the cycle (mint, since Part 7 was blue). Part 9 (slide 100) then becomes purple, Part 10 (slide 113) becomes orange. Concretely:

| Slide | Part | Bloom |
|---:|---|---|
| 7 | 1 | purple |
| 15 | 2 | orange |
| 28 | 3 | blue |
| 42 | 4 | mint |
| 60 | 5 | purple |
| 72 | 6 | orange |
| 81 | 7 | blue |
| 95 | 8 (NEW) | **mint** |
| 100 | 9 (was 8) | **purple** |
| 113 | 10 (was 9) | **orange** |

The section-title text inside slides 100 and 113 also needs its part-number updated (was "8 · Getting iOS engineers…" / "9 · Results"; becomes "9 · Getting iOS engineers…" / "10 · Results"). If any other slide in those sections references the part number on-screen (e.g. a "back to Part 8" beat), update too.

---

## 3. Files to update — sync impact

| File | What changes |
|---|---|
| `presentation/index.html` | All slide changes per §1, full renumber cascade, section-title part-number updates on old Parts 8 & 9, blooms on new section titles. |
| `PRESENTATION_PLAN.md` | All slide entries per §1 + slide-count summary table at the bottom (125 slides). |
| `iOS-ARCHITECTURE.md` | Slide pointer paragraph updates. Was `~47, 48, 50, 51` in v3 → new positions: slide 14 (two-architectures-detailed), slide 46 (was 48, module graph), slides 48 + 49 (were 50/51, code). New pointer: `~14, ~46, ~48, ~49`. |
| `ASSETS.md` | Add `assets/diagrams/kmp-integration.png` to the "Wired in v3.1" list. Note that slides 46/47 references for old `android-architecture.png` and `ios-architecture.png` are no longer wired (the assets stay in the repo but unreferenced). |
| `presentation/css/deck.css` | One small addition: `.contacts-avatar` class for the closing-slide portrait (rounded, 96px). |

No changes to `code-examples/` (frozen).

---

## 4. Open decisions / risks

1. **Slide 46/47 "28 is enough" interpretation.** I'm reading "28" as a typo for either 14 or 48 — the substance is "the side-by-side comparison slides carry this beat well enough." If you actually meant slide 28 (Part 3 section title), tell me and I'll reconsider.

2. **Android/iOS distribution claim verification.** A research subagent is checking JetBrains' KMP docs. If the claim turns out wrong or more nuanced, slide 98's speaker note tightens. The visual stays.

3. **Bloom color for new Part 8.** Continuing the existing rotation (purple/orange/blue/mint repeating) gives Part 8 = **mint**. Part 9 (was Part 8) becomes **purple**; Part 10 (was 9) becomes **orange**. This means existing Parts 8 & 9 section-title bloom colors *change* under the renumber. If you'd rather keep a section's bloom color stable across iterations, say so.

4. **Avatar size on closing slide.** I'm planning 96px circular. If you'd rather have it bigger (~160px) or in a different position (e.g. left of QR), say so.

5. **The 5-slide new Part 8 count.** Could compress to 3–4 slides if pacing is tight. The 5-slide layout above gives one beat per idea (section title / options / answer / picture / CI). Cutting to 3 would mean: section title / picture / "CI ships it." (skipping the explicit "options" frame and the standalone "Library." word).

---

## 5. Implementation outline

Same shape as the v3 plan: one commit per task, top-down by slide number, renumber cascade computed up front.

Tasks (one commit each):

1. `v3.1 css` — add `.contacts-avatar` to `deck.css`.
2. `v3.1 slide 1` — drop event-card, switch date to English.
3. `v3.1 part 4` — drop slides 46, 47; cascade renumber.
4. `v3.1 part 5` — drop slide 69; cascade renumber.
5. `v3.1 part 6` — insert 2 new slides (bullets + bigword), drop old AiService code slides 82/83; net 0 count change.
6. `v3.1 new part 8` — insert 5 new slides between Part 7 and old Part 8; renumber old Parts 8 and 9 in section-title text + plan summary; bloom on new section title.
7. `v3.1 closing avatar` — add `<img>` portrait to slide 125.
8. `v3.1 sync` — update `iOS-ARCHITECTURE.md`, `ASSETS.md`, slide-count summary; tag `v3.1`.
